import React, {Component}   from 'react';
import styled               from 'styled-components';

import {Button,
        Title,
        Main }             from '../components';

export default class MainViewPresenter extends Component {

    constructor(props) {
        super(props);
    }

    render() {
        return (
            <div> MAIN VIEW PRESENTER BUTT HEAD </div>
        )
    }
}
